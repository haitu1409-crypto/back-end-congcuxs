# Tối ưu Puppeteer cho Render deployment
puppeteer_skip_chromium_download=true
puppeteer_download_host=https://npm.taobao.org/mirrors

# Tối ưu npm install
fund=false
audit=false
progress=false
loglevel=error

# Cache settings
cache-max=86400000
cache-min=3600000

# Timeout settings
timeout=300000